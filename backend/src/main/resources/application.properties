# Datos de conexión (basados en nuestro docker-compose)
spring.datasource.url=jdbc:postgresql://localhost:5432/capacitaciones_db
spring.datasource.username=user_admin
spring.datasource.password=password_123

# Hibernate: Esto crea las tablas automáticamente por nosotros
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect